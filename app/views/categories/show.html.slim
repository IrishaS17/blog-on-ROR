=render 'shared/breadcrumb', category: @category
h1 =t('.category') + @category.name
section#posts
  -if @posts.present?
    =render @posts
  end
=will_paginate @posts, :renderer => BootstrapPaginationHelper::LinkRenderer